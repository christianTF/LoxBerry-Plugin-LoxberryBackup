# These packages will be installed by apt-get -y install PACKAGENAME
# One line per package, use exact packagename as you would do for apt-get.
#
# Squeezelite from the Debian repository 
# (will possibly be changed in the future for a newer Squeezelite version) 

libio-interface-perl libswitch-perl libio-socket-timeout-perl
